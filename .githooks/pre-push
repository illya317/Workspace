#!/bin/sh
# Pre-push hook: keep local push checks short; full validation belongs to CI/server.

echo "→ Running quick push checks..."
npm run check:quick || {
  echo "✗ Quick push checks failed. Push aborted."
  exit 1
}

echo "✓ Quick push checks passed."
