#!/bin/sh
# Pre-push hook: run the full local suite before code leaves the machine.

echo "→ Running full local push checks..."
npm run ci || {
  echo "✗ Local push checks failed. Push aborted."
  exit 1
}

echo "✓ Local push checks passed."
