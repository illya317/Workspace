// Generated by scripts/deploy/deploy-unit-app-generator.ts. Do not edit.
import { RelatedPartiesClient } from "@workspace/external/ui";
import { requireRouteAccess } from "@workspace/platform/server/auth";
import { evaluatePermissionAction } from "@workspace/platform/server/rbac/action-grants";
import { renderAppShellPage } from "@workspace/platform/ui/app-shell-page";

export default async function RelatedPartiesPage() {
  const user = await requireRouteAccess("/external/related-parties");
  const canCreate = await evaluatePermissionAction(user.id, "external.relatedParties", "create");
  const canDelete = await evaluatePermissionAction(user.id, "external.relatedParties", "delete");
  return renderAppShellPage({
    title: "关联方",
    backHref: "/external",
    user,
    children: <RelatedPartiesClient canCreate={canCreate} canDelete={canDelete} />,
  });
}
