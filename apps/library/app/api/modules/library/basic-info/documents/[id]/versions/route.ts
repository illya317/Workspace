// Generated by scripts/deploy/deploy-unit-app-generator.ts. Do not edit.
import { z } from "zod";

import {
  buildUploadLibraryDocumentVersionCommand,
  executeLibraryDocumentVersionsCommand,
  executeUploadLibraryDocumentVersionCommand,
} from "@workspace/library/server/route-commands";import { routeIdParamsSchema } from "@workspace/platform/server/api";
import { createCommandRoute } from "@workspace/platform/server/api-route";import { okCommand } from "@workspace/platform/server/domain-validation";

export const GET = createCommandRoute({
  paramsSchema: routeIdParamsSchema,
  paramsError: "Invalid id",
  buildCommand: ({ params, user }) => okCommand({ id: params.id, userId: user.userId }),
  action: executeLibraryDocumentVersionsCommand,
});

const uploadVersionSchema = z.object({
  file: z.instanceof(File),
  versionLabel: z.string().optional(),
  changeNote: z.string().optional(),
});

export const POST = createCommandRoute({
  accessError: "No import permission",
  paramsSchema: routeIdParamsSchema,
  paramsError: "Invalid id",
  bodyParser: "formData",
  bodySchema: uploadVersionSchema,
  bodyError: "Please upload a valid file",
  buildCommand: ({ params, body, user }) => buildUploadLibraryDocumentVersionCommand({
    id: params.id,
    userId: user.userId,
    body,
  }),
  action: executeUploadLibraryDocumentVersionCommand,
});
