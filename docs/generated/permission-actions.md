<!-- AUTO-GENERATED by scripts/generate-permission-action-docs.ts. DO NOT EDIT. -->

# 全项目权限 Action 授权手册

当前共 20 个 permission action、105 个资源策略、212 个已注册 BusinessAction。

事实来源：`action-registry.ts`、`permission-resource-policy.ts`、`module-registry.ts` 与 `business-action-registry.ts`。业务写入的状态、校验和持久化细节继续以 `action-contracts.md` 为准。

## 授权前先看这四条

1. 永远按完整的 `resource.action` 授权，不能只看 `approve`、`update` 这样的 action 名。
2. `approve` 可能表示审批、复核、启用、发布或关闭表决；下方的业务绑定才是它在当前资源里的真实含义。
3. `显式配置` 只表示不能从父资源继承，仍然可以配置给用户、岗位或部门；岗位/部门成员会获得有效权限。
4. 有 action 权限不等于能操作所有对象。scope、对象归属、状态机、职责分离和 service guard 仍会继续收紧。

相关文档：[`RBAC 权限模型`](../engineering/security/rbac.md) · [`权限矩阵`](../engineering/security/permission-matrix.md) · [`ActionContract Registry`](./action-contracts.md)

## 定期复查与异常通报

生产环境由发布运维 Agent 按租户业务时区每日 08:00 全量复查；后台授权事务提交后还会立即执行一次复查。两条路径共用同一份租户核准基线和异常判定，不会各自维护规则。

复查覆盖：L1/L2 新增、移除和归属转移；任意未经核准的显式授权与漏撤销；直接授权用户调岗；持权岗位/部门换人；停用主体或停用资源仍有授权；非法或资源不支持的 action；提交/填报与审批/复核职责未分离；隐式全资源授权管理员岗位变化。异常会按 action 风险分级，但普通 `entry/read/update` 错授也会立即通报。

真正的错授、漏撤、停用主体残留等异常会形成需要确认的站内强提醒，并在未解决期间按 24 小时再次提醒。提交/审批资格重合只形成一次普通流程提示：两类权限可以授给同一人，具体单据由 workflow/service 阻止提交人本人处理。所有结果都会写入服务器结构化日志。

合法新增 L2、资源转移或显式授权变更，必须先由权限责任人复核，再运行 `npm run --silent permission-review:baseline` 生成纯 JSON 候选基线并提交租户配置；不能通过自动采纳当前数据库来消除报警。只读核查使用 `npm run permission-review:check`。

## Action 通用字典

Action 只定义授权类别，不承诺跨资源具有同一个业务结果。最终含义必须读取完整的 `resource.action`。

| Action | 分组 | 通用含义 | 自动包含 | 风险级别 |
|---|---|---|---|---|
| `entry` 进入 | basic | 进入资源对应的页面、菜单或功能入口。 | 无 | 基础 |
| `read` 查看 | basic | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | `entry` | 基础 |
| `create` 新建 | basic | 创建该资源中的新记录或业务草稿。 | `entry`、`read` | 基础 |
| `update` 编辑 | basic | 修改该资源中已经存在且当前状态允许编辑的记录。 | `entry`、`read` | 基础 |
| `delete` 删除 | basic | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | `entry`、`read` | 高：按完整权限核对 |
| `archive` 归档 | lifecycle | 归档或反归档仍需保留的历史事实，不等于删除。 | `entry`、`read` | 高：按完整权限核对 |
| `revise` 修订 | lifecycle | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | `entry`、`read` | 高：按完整权限核对 |
| `reverse` 撤销 | lifecycle | 撤销、作废、冲销或执行反向业务处理；具体结果必须看资源内的业务动作。 | `entry`、`read` | 高：按完整权限核对 |
| `lock` 锁定 | lifecycle | 关闭期间、批次或写入窗口，阻止后续普通写入。 | `entry`、`read` | 高：按完整权限核对 |
| `unlock` 解锁 | lifecycle | 重新打开期间、批次或写入窗口。 | `entry`、`read` | 高：按完整权限核对 |
| `submit` 提交 | workflow | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | `entry`、`read` | 高：按完整权限核对 |
| `approve` 审批通过 | workflow | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | `entry`、`read` | 高：按完整权限核对 |
| `reject` 审批驳回 | workflow | 驳回流程或业务申请；approve 不会自动包含 reject。 | `entry`、`read` | 高：按完整权限核对 |
| `import` 导入 | exchange | 导入、摄取或批量确认外部数据；默认不自动包含 create/update。 | 无 | 高：按完整权限核对 |
| `export` 导出 | exchange | 导出、下载、打印或对外发送该资源数据。 | `entry`、`read` | 高：按完整权限核对 |
| `apiUse` API 调用 | exchange | 通过集成 API 使用该资源能力；默认不自动包含业务读写。 | 无 | 高：按完整权限核对 |
| `share` 共享 | governance | 把单条记录共享给其他主体或建立临时协作访问。 | `entry`、`read` | 高：按完整权限核对 |
| `grant` 授权 | governance | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | 无 | 高：按完整权限核对 |
| `configure` 配置 | governance | 修改规则、流程或系统配置；默认不自动包含业务数据读写。 | 无 | 高：按完整权限核对 |
| `audit` 审计 | governance | 查看审计、变更或权限台账；默认不自动包含业务数据读写。 | 无 | 高：按完整权限核对 |

## `approve` 到底是什么意思

`approve` 不是一个全局岗位，也不是 capability 名称。它是挂在具体业务资源上的高风险 action。它自动带来该资源的 `read + entry`，但不会带来 `submit`、`reject`、`create` 或 `update`。

| 完整权限 | 资源 | 直接动作 / 流程资格 | 建议授权对象 | 配置方式 |
|---|---|---|---|---|
| `docs.editor.approve` | 模板编辑器 | 流程处理资格：可作为“创建文档模板草稿”所发起流程的处理人（`docs.editor.template.draft.create`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“保存文档模板草稿”所发起流程的处理人（`docs.editor.template.draft.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“发布文档模板”所发起流程的处理人（`docs.editor.template.publish`；具体处理接口和对象范围仍由 workflow/service 决定） | 对应组织空间内的模板审批或发布责任人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `finance.budget.approve` | 预算管理 | 直接执行：启用预算版本（`finance.budget.version.activate`；POST /api/modules/finance/budget/versions/:id/activate） | 有权启用预算版本的预算负责人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `finance.ledger.approve` | 总账会计 | 直接执行：复核集团科目（`finance.ledger.groupAccount.review`；POST /api/modules/finance/ledger/group-accounts/:id/review）<br>直接执行：复核关账底稿（`finance.ledger.close.workpaper.review`；POST /api/modules/finance/ledger/closing/workpapers/review） | 集团科目或总账治理的复核责任人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `finance.statements.approve` | 财务报表 | 直接执行：通过合并抵销分录（`finance.statements.consolidationEntry.approve`；POST /api/modules/finance/statements/consolidation/batches/:batchId/entries/:entryId/approve）<br>直接执行：独立复核合并批次（`finance.statements.consolidationBatch.review`；POST /api/modules/finance/statements/consolidation/batches/:batchId/review）<br>直接执行：发布合并报表（`finance.statements.consolidationBatch.publish`；POST /api/modules/finance/statements/consolidation/batches/:batchId/publish） | 合并抵销、合并批次复核与报表发布责任人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `hr.performance.approve` | 绩效管理 | 流程处理资格：可作为“发起绩效评审”所发起流程的处理人（`hr.performance.review.evaluate`；具体处理接口和对象范围仍由 workflow/service 决定） | 绩效流程的正式审批责任人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `hr.roster.approve` | 人事基础资料 | 流程处理资格：可作为“创建部门”所发起流程的处理人（`hr.roster.department.create`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“更新部门”所发起流程的处理人（`hr.roster.department.update`；具体处理接口和对象范围仍由 workflow/service 决定） | 组织、人事资料审批责任人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `inventory.receipts.approve` | 成品入库报单 | 直接执行：复核成品入库报单月度汇总（`inventory.receipts.report.review`；POST /api/modules/inventory/receipts/reports/:reportId/review）<br>流程处理资格：可作为“确认成品入库报单月度汇总”所发起流程的处理人（`inventory.receipts.report.confirm`；具体处理接口和对象范围仍由 workflow/service 决定） | 财务复核人员；可同时具备制表资格，但不得复核本人制表的单据。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `production.qc.approve` | 批次检验 | 直接执行：QC 批次复核（`production.qc.batch.review`；POST /api/modules/production/qc/:batchId/approve-review）<br>流程处理资格：可作为“保存 QC 预检记录”所发起流程的处理人（`production.qc.batch.precheck.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“保存 QC 检验记录”所发起流程的处理人（`production.qc.batch.inspection.save`；具体处理接口和对象范围仍由 workflow/service 决定） | QC 独立复核人员；不应由同一批次的填报人兼任。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `work.meetings.approve` | 会议管理 | 直接执行：关闭会议表决（`work.meetings.vote.close`；POST /api/modules/work/meetings/:id/votes/:proposalId/close） | 被授权关闭表决并形成结果的会议治理责任人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `work.projects.approve` | 项目管理 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 项目发起流程中对应赋能部门的负责人或正式审批人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |
| `work.tasks.approve` | 工作计划 | 流程处理资格：可作为“创建工作节点”所发起流程的处理人（`work.tasks.item.create`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“更新工作节点”所发起流程的处理人（`work.tasks.item.update`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交工作汇报/绩效确认”所发起流程的处理人（`work.tasks.report.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交修订/更正”所发起流程的处理人（`work.tasks.revision.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门期初目标提交”所发起流程的处理人（`work.tasks.goal.department.objective.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人期初目标提交”所发起流程的处理人（`work.tasks.goal.personal.objective.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门考核结果提交”所发起流程的处理人（`work.tasks.goal.department.report.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人考核结果提交”所发起流程的处理人（`work.tasks.goal.personal.report.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门期初目标修订”所发起流程的处理人（`work.tasks.goal.department.objective.revise`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人期初目标修订”所发起流程的处理人（`work.tasks.goal.personal.objective.revise`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门考核结果修订”所发起流程的处理人（`work.tasks.goal.department.report.correct`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人考核结果修订”所发起流程的处理人（`work.tasks.goal.personal.report.correct`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交部门协作”所发起流程的处理人（`work.tasks.collaboration.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交目标审查”所发起流程的处理人（`work.tasks.objective_plan.save`；具体处理接口和对象范围仍由 workflow/service 决定） | 目标、计划、汇报或协作事项的实际审批责任人；避免同时给同一对象的提交人与审批人。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 |

## 全项目资源 Action 清单

每一行都是一个可独立判断的权限点。没有业务绑定不等于没有权限效果，只表示当前效果主要来自页面/API guard 或对象级 service；这种权限在授权前更需要查看资源说明。

### 人事管理（`hr`）

#### 人事管理（`hr`）

类型：容器资源 · 页面：`/hr` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `hr.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `hr.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `hr.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `hr.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `hr.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `hr.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 人力分析（`hr.analytics`）

类型：业务资源 · 页面：`/hr/analytics` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `hr.analytics.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `hr.analytics.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `hr.analytics.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 绩效管理（`hr.performance`）

类型：业务资源 · 页面：`/hr/performance` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `hr.performance.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `hr.performance.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `hr.performance.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.performance.reverse`<br>撤销 | 撤销、作废、冲销或执行反向业务处理；具体结果必须看资源内的业务动作。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.performance.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 流程发起资格：可发起“发起绩效评审”对应流程（`hr.performance.review.evaluate`；具体对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.performance.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 流程处理资格：可作为“发起绩效评审”所发起流程的处理人（`hr.performance.review.evaluate`；具体处理接口和对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.performance.reject`<br>审批驳回 | 驳回流程或业务申请；approve 不会自动包含 reject。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.performance.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 人事基础资料（`hr.roster`）

类型：业务资源 · 页面：`/hr/roster` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `hr.roster.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `hr.roster.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `hr.roster.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建员工档案（`hr.roster.employee.create`；POST /api/modules/hr/roster/employees）<br>直接执行：创建部门（`hr.roster.department.create`；POST /api/modules/hr/roster/departments）<br>直接执行：创建岗位（`hr.roster.position.create`；POST /api/modules/hr/roster/positions） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `hr.roster.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：保存员工信息表（`hr.roster.employee.update`；PUT /api/modules/hr/roster/employees）<br>直接执行：登记员工生命周期变更（`hr.roster.employeeProfile.lifecycle.record`；PUT /api/modules/hr/roster/employee-profiles/:id/lifecycle）<br>直接执行：更新部门（`hr.roster.department.update`；PUT /api/modules/hr/roster/departments、PUT /api/modules/hr/roster/departments/:id）<br>直接执行：更新岗位（`hr.roster.position.update`；PUT /api/modules/hr/roster/positions/:id、PUT /api/modules/hr/roster/positions）<br>直接执行：维护员工合同与期限（`hr.roster.employmentAgreement.command`；POST /api/modules/hr/roster/employee-profiles/:id/agreements）<br>直接执行：办理员工社会保险（`hr.roster.socialInsurance.command`；POST /api/modules/hr/roster/employee-profiles/:id/social-insurance）<br>直接执行：上传员工协议附件（`hr.roster.employmentAgreementAttachment.upload`；POST /api/modules/hr/roster/employee-profiles/:id/agreements/:agreementUid/attachments）<br>直接执行：移除员工协议附件（`hr.roster.employmentAgreementAttachment.remove`；POST /api/modules/hr/roster/employee-profiles/:id/agreements/:agreementUid/attachments/:attachmentUid/remove）<br>直接执行：修正人员任职资料（`hr.roster.employeePeriod.correct`；PATCH /api/modules/hr/roster/employee-profiles/:id/periods/:periodId）<br>直接执行：新增员工任职记录（`hr.roster.employeeAssignment.create`；POST /api/modules/hr/roster/employee-profiles/:id/assignments）<br>直接执行：保存部门编码（`hr.roster.departmentCode.save`；PUT /api/modules/hr/roster/department-codes）<br>直接执行：修正员工雇佣资料（`hr.roster.employment.update`；PUT /api/modules/hr/roster/employments）<br>直接执行：新增员工雇佣期间（`hr.roster.employment.create`；POST /api/modules/hr/roster/employments）<br>直接执行：保存岗位编码（`hr.roster.positionCode.save`；PUT /api/modules/hr/roster/position-codes）<br>直接执行：保存岗位说明模板（`hr.roster.positionDescriptionTemplate.save`；PUT /api/modules/hr/roster/position-description-templates）<br>直接执行：保存岗位说明（`hr.roster.positionDescription.save`；PUT /api/modules/hr/roster/position-descriptions）<br>直接执行：保存岗位汇报关系覆盖（`hr.roster.positionReportOverride.save`；PUT /api/modules/hr/roster/position-report-overrides） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `hr.roster.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除部门（`hr.roster.department.delete`；DELETE /api/modules/hr/roster/departments、DELETE /api/modules/hr/roster/departments/:id）<br>直接执行：删除岗位（`hr.roster.position.delete`；DELETE /api/modules/hr/roster/positions/:id、DELETE /api/modules/hr/roster/positions）<br>直接执行：删除部门编码（`hr.roster.departmentCode.delete`；DELETE /api/modules/hr/roster/department-codes）<br>直接执行：删除岗位编码（`hr.roster.positionCode.delete`；DELETE /api/modules/hr/roster/position-codes） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `hr.roster.archive`<br>归档 | 归档或反归档仍需保留的历史事实，不等于删除。 | 直接执行：归档部门（`hr.roster.department.archive`；POST /api/modules/hr/roster/departments/:id/archive）<br>直接执行：归档岗位（`hr.roster.position.archive`；POST /api/modules/hr/roster/positions/:id/archive） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.roster.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | 直接执行：恢复人事历史版本（`hr.roster.audit.restore`；POST /api/modules/hr/roster/audit-log/restore） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.roster.reverse`<br>撤销 | 撤销、作废、冲销或执行反向业务处理；具体结果必须看资源内的业务动作。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.roster.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 流程发起资格：可发起“创建部门”对应流程（`hr.roster.department.create`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“更新部门”对应流程（`hr.roster.department.update`；具体对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.roster.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 流程处理资格：可作为“创建部门”所发起流程的处理人（`hr.roster.department.create`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“更新部门”所发起流程的处理人（`hr.roster.department.update`；具体处理接口和对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.roster.reject`<br>审批驳回 | 驳回流程或业务申请；approve 不会自动包含 reject。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.roster.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 花名册生成资料（`hr.roster.generated`）

类型：独立 capability · 页面：`/hr/roster` · owner：`hr.roster` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `hr.roster.generated.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `hr.roster.generated.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `hr.roster.generated.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | 直接执行：导出花名册生成资料（`hr.roster.generated.export`；GET /api/modules/hr/roster/generated/export） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `hr.roster.generated.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 外部关系（`external`）

#### 外部关系（`external`）

类型：容器资源 · 页面：`/external` · scope：全局

资源说明：External owns customer, supplier, and related-party master views; customer and supplier roles can contain organizations or individuals.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `external.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `external.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `external.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `external.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `external.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `external.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 客户管理（`external.customers`）

类型：业务资源 · 页面：`/external/customers` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `external.customers.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `external.customers.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `external.customers.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建客户（`external.customers.party.create`；POST /api/modules/external/customers） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.customers.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新客户（`external.customers.party.update`；PATCH /api/modules/external/customers/:id）<br>直接执行：登记客户角色可用期间（`external.customers.party.availability.change`；POST /api/modules/external/customers/:id/availability） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.customers.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：停用客户角色（`external.customers.party.delete`；DELETE /api/modules/external/customers/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.customers.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 关联方（`external.relatedParties`）

类型：业务资源 · 页面：`/external/related-parties` · scope：全局

资源说明：Related-party directory derived from ExternalPartyProfile; create classifies an existing readable customer or supplier Party FK. delete only resets manually maintained classifications to unrelated and preserves Party/roles; internal companies and current ownership-derived relationships are protected.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `external.relatedParties.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `external.relatedParties.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `external.relatedParties.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：登记关联方（`external.relatedParties.party.create`；POST /api/modules/external/related-parties） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.relatedParties.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：取消关联方（`external.relatedParties.party.delete`；DELETE /api/modules/external/related-parties/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.relatedParties.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 供应商管理（`external.suppliers`）

类型：业务资源 · 页面：`/external/suppliers` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `external.suppliers.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `external.suppliers.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `external.suppliers.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建供应商（`external.suppliers.party.create`；POST /api/modules/external/suppliers） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.suppliers.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新供应商（`external.suppliers.party.update`；PATCH /api/modules/external/suppliers/:id）<br>直接执行：登记供应商角色可用期间（`external.suppliers.party.availability.change`；POST /api/modules/external/suppliers/:id/availability） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.suppliers.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：停用供应商角色（`external.suppliers.party.delete`；DELETE /api/modules/external/suppliers/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `external.suppliers.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 存货管理（`inventory`）

#### 存货管理（`inventory`）

类型：容器资源 · 页面：`/inventory` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `inventory.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `inventory.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `inventory.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `inventory.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `inventory.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `inventory.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 库存运营（`inventory.operations`）

类型：业务资源 · 页面：`/inventory/operations` · scope：全局

资源说明：产品/SKU 主数据由 production.products 维护；create=库存单据草稿，update=单据过账，reverse=反向流水冲销，lock/unlock=期间结转治理。

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `inventory.operations.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `inventory.operations.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `inventory.operations.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建存货单据（`inventory.operations.document.create`；POST /api/modules/inventory/operations/documents） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `inventory.operations.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：过账存货单据（`inventory.operations.document.post`；POST /api/modules/inventory/operations/documents/:id/post） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `inventory.operations.reverse`<br>撤销 | 撤销、作废、冲销或执行反向业务处理；具体结果必须看资源内的业务动作。 | 直接执行：冲销存货单据（`inventory.operations.document.reverse`；POST /api/modules/inventory/operations/documents/:id/reverse） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `inventory.operations.lock`<br>锁定 | 关闭期间、批次或写入窗口，阻止后续普通写入。 | 直接执行：关联存货结转凭证（`inventory.operations.closing.linkVoucher`；POST /api/modules/inventory/operations/closing/link-voucher） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `inventory.operations.unlock`<br>解锁 | 重新打开期间、批次或写入窗口。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `inventory.operations.import`<br>导入 | 导入、摄取或批量确认外部数据；默认不自动包含 create/update。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `inventory.operations.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `inventory.operations.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 成品入库报单（`inventory.receipts`）

类型：业务资源 · 页面：`/inventory/receipts` · scope：全局

资源说明：车间填写月度投料、产量、产品工分和包装折合；submit=车间确认月度汇总，approve=财务独立复核，折合值由服务端按源字段计算。

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `inventory.receipts.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `inventory.receipts.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `inventory.receipts.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：新增成品入库报单记录（`inventory.receipts.record.create`；POST /api/modules/inventory/receipts） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `inventory.receipts.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新成品入库报单记录（`inventory.receipts.record.update`；PATCH /api/modules/inventory/receipts/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `inventory.receipts.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除成品入库报单记录（`inventory.receipts.record.delete`；DELETE /api/modules/inventory/receipts/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `inventory.receipts.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 直接执行：确认成品入库报单月度汇总（`inventory.receipts.report.confirm`；POST /api/modules/inventory/receipts/reports/:reportId/confirm）<br>流程发起资格：可发起“确认成品入库报单月度汇总”对应流程（`inventory.receipts.report.confirm`；具体对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `inventory.receipts.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 直接执行：复核成品入库报单月度汇总（`inventory.receipts.report.review`；POST /api/modules/inventory/receipts/reports/:reportId/review）<br>流程处理资格：可作为“确认成品入库报单月度汇总”所发起流程的处理人（`inventory.receipts.report.confirm`；具体处理接口和对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `inventory.receipts.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 工作管理（`work`）

#### 工作管理（`work`）

类型：容器资源 · 页面：`/work` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `work.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `work.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `work.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `work.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `work.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `work.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 会议管理（`work.meetings`）

类型：业务资源 · 页面：`/work/meeting` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `work.meetings.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `work.meetings.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `work.meetings.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建会议（`work.meetings.meeting.create`；POST /api/modules/work/meetings） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.meetings.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新会议（`work.meetings.meeting.update`；PUT /api/modules/work/meetings/:id）<br>直接执行：处理会议行动候选（`work.meetings.actionCandidate.process`；POST /api/modules/work/meetings/:id/action-candidates）<br>直接执行：添加会议议程（`work.meetings.agenda.create`；POST /api/modules/work/meetings/:id/agenda）<br>直接执行：添加会议决议（`work.meetings.decision.create`；POST /api/modules/work/meetings/:id/decisions）<br>直接执行：添加会议纪要（`work.meetings.minute.create`；POST /api/modules/work/meetings/:id/minutes）<br>直接执行：保存会议参会人（`work.meetings.participant.save`；POST /api/modules/work/meetings/:id/participants）<br>直接执行：添加会议议案（`work.meetings.proposal.create`；POST /api/modules/work/meetings/:id/proposals） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.meetings.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除会议（`work.meetings.meeting.delete`；DELETE /api/modules/work/meetings/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.meetings.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 直接执行：提交会议表决（`work.meetings.vote.cast`；POST /api/modules/work/meetings/:id/votes/:proposalId/cast） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.meetings.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 直接执行：关闭会议表决（`work.meetings.vote.close`；POST /api/modules/work/meetings/:id/votes/:proposalId/close） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.meetings.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 会议全量查看（`work.meetings.viewAll`）

类型：独立 capability · 页面：`/work/meeting` · owner：`work.meetings` · scope：全局

资源说明：Allows reading all meetings after the work.meetings owner entry gate; ordinary users remain limited by visibility and participation.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `work.meetings.viewAll.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `work.meetings.viewAll.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 项目管理（`work.projects`）

类型：业务资源 · 页面：`/work/project` · scope：`personal`、`company`、`committee`、`department`

资源说明：Project data uses the standard personal/department/committee/company space scopes; project creation uses submit plus enabling-department owner approve/reject, while active-project actions remain space-scoped.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `work.projects.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `work.projects.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `work.projects.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：添加项目成员（`work.projects.member.create`；POST /api/modules/work/projects/members）<br>直接执行：创建项目阶段（`work.projects.phase.create`；POST /api/modules/work/projects/:id/plan-phases）<br>直接执行：创建项目计划基线（`work.projects.baseline.create`；POST /api/modules/work/projects/:id/plan-baselines） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.projects.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新项目（`work.projects.project.update`；PUT /api/modules/work/projects/:id）<br>直接执行：更新项目成员（`work.projects.member.update`；PUT /api/modules/work/projects/members/:id）<br>直接执行：更新项目阶段（`work.projects.phase.update`；PUT /api/modules/work/projects/:id/plan-phases/:phaseId）<br>直接执行：保存项目甘特（`work.projects.planGantt.save`；PUT /api/modules/work/projects/:id/plan-gantt、PUT /api/modules/work/projects/:id/plan-dependencies） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.projects.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除项目（`work.projects.project.delete`；DELETE /api/modules/work/projects/:id）<br>直接执行：结束项目成员关系（`work.projects.member.delete`；DELETE /api/modules/work/projects/members/:id）<br>直接执行：删除项目阶段（`work.projects.phase.delete`；DELETE /api/modules/work/projects/:id/plan-phases/:phaseId） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.projects.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | 直接执行：启用项目计划基线（`work.projects.baseline.activate`；POST /api/modules/work/projects/:id/plan-baselines/:baselineId/activate） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.projects.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.projects.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.projects.reject`<br>审批驳回 | 驳回流程或业务申请；approve 不会自动包含 reject。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.projects.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 项目发起（`work.projects.initiate`）

类型：独立 capability · 页面：`/work/project` · owner：`work.projects` · scope：全局

资源说明：Global project initiation capability; owner entry is still required and project workflow/object services enforce target-level rules.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `work.projects.initiate.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 流程发起资格：可发起“提交项目确认”对应流程（`work.projects.project.create`；具体对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.projects.initiate.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 工作计划（`work.tasks`）

类型：业务资源 · 页面：`/work/me` · scope：`personal`、`company`、`committee`、`department`、`project`

资源说明：Work task data uses personal, organization and project space scopes; project execution permissions can be granted directly on the concrete project space.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `work.tasks.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `work.tasks.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `work.tasks.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：保存工作计划草稿（`work.tasks.plan.create`；POST /api/modules/work/tasks/plans）<br>直接执行：创建工作节点（`work.tasks.item.create`；POST /api/modules/work/tasks）<br>直接执行：创建周期工作安排（`work.tasks.periodSchedule.create`；POST /api/modules/work/tasks/period-schedule） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.tasks.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：保存工作计划草稿修改（`work.tasks.plan.save`；PUT /api/modules/work/tasks/plans/:id）<br>直接执行：更新工作节点（`work.tasks.item.update`；PUT /api/modules/work/tasks/:id）<br>直接执行：创建 KPI 指标定义（`work.tasks.kpi_definition.create`；POST /api/modules/work/tasks/kpi/definitions）<br>直接执行：修订 KPI 指标定义（`work.tasks.kpi_definition.revise`；PUT /api/modules/work/tasks/kpi/definitions/:id）<br>直接执行：更新 KPI 实际值（`work.tasks.kpi_measurement.update`；PUT /api/modules/work/tasks/plans/:id/kpi-measurements）<br>直接执行：提交工作汇报/绩效确认（`work.tasks.report.save`；PUT /api/modules/work/tasks/reports、POST /api/modules/work/tasks/submissions、POST /api/modules/work/tasks/submissions/:id/submit）<br>直接执行：提交修订/更正（`work.tasks.revision.save`；POST /api/modules/work/tasks/submissions）<br>直接执行：部门期初目标提交（`work.tasks.goal.department.objective.submit`；PUT /api/modules/work/tasks/plans/:id/kpi-scorecard、POST /api/modules/work/tasks/submissions、POST /api/modules/work/tasks/submissions/:id/submit）<br>直接执行：个人期初目标提交（`work.tasks.goal.personal.objective.submit`；PUT /api/modules/work/tasks/plans/:id/kpi-scorecard、POST /api/modules/work/tasks/submissions、POST /api/modules/work/tasks/submissions/:id/submit）<br>直接执行：部门考核结果提交（`work.tasks.goal.department.report.submit`；PUT /api/modules/work/tasks/reports、POST /api/modules/work/tasks/submissions、POST /api/modules/work/tasks/submissions/:id/submit）<br>直接执行：个人考核结果提交（`work.tasks.goal.personal.report.submit`；PUT /api/modules/work/tasks/reports、POST /api/modules/work/tasks/submissions、POST /api/modules/work/tasks/submissions/:id/submit）<br>直接执行：部门期初目标修订（`work.tasks.goal.department.objective.revise`；POST /api/modules/work/tasks/submissions）<br>直接执行：个人期初目标修订（`work.tasks.goal.personal.objective.revise`；POST /api/modules/work/tasks/submissions）<br>直接执行：部门考核结果修订（`work.tasks.goal.department.report.correct`；POST /api/modules/work/tasks/submissions）<br>直接执行：个人考核结果修订（`work.tasks.goal.personal.report.correct`；POST /api/modules/work/tasks/submissions）<br>直接执行：提交部门协作（`work.tasks.collaboration.submit`；POST /api/modules/work/tasks/collaborations、PUT /api/modules/work/tasks/collaborations/:id）<br>直接执行：响应部门协作（`work.tasks.collaboration.respond`；POST /api/modules/work/tasks/collaborations/:id/respond）<br>直接执行：提交目标审查（`work.tasks.objective_plan.save`；POST /api/modules/work/tasks/submissions、POST /api/modules/work/tasks/submissions/:id/submit） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.tasks.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除 OKR/工作计划（`work.tasks.plan.delete`；DELETE /api/modules/work/tasks/plans/:id/delete）<br>直接执行：删除工作节点（`work.tasks.item.delete`；DELETE /api/modules/work/tasks/:id）<br>直接执行：删除 KPI 指标定义（`work.tasks.kpi_definition.delete`；DELETE /api/modules/work/tasks/kpi/definitions/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `work.tasks.archive`<br>归档 | 归档或反归档仍需保留的历史事实，不等于删除。 | 直接执行：归档 OKR/工作计划（`work.tasks.plan.archive`；DELETE /api/modules/work/tasks/plans/:id） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.tasks.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.tasks.reverse`<br>撤销 | 撤销、作废、冲销或执行反向业务处理；具体结果必须看资源内的业务动作。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.tasks.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 流程发起资格：可发起“创建工作节点”对应流程（`work.tasks.item.create`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“更新工作节点”对应流程（`work.tasks.item.update`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“提交工作汇报/绩效确认”对应流程（`work.tasks.report.save`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“提交修订/更正”对应流程（`work.tasks.revision.save`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“部门期初目标提交”对应流程（`work.tasks.goal.department.objective.submit`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“个人期初目标提交”对应流程（`work.tasks.goal.personal.objective.submit`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“部门考核结果提交”对应流程（`work.tasks.goal.department.report.submit`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“个人考核结果提交”对应流程（`work.tasks.goal.personal.report.submit`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“部门期初目标修订”对应流程（`work.tasks.goal.department.objective.revise`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“个人期初目标修订”对应流程（`work.tasks.goal.personal.objective.revise`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“部门考核结果修订”对应流程（`work.tasks.goal.department.report.correct`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“个人考核结果修订”对应流程（`work.tasks.goal.personal.report.correct`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“提交部门协作”对应流程（`work.tasks.collaboration.submit`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“提交目标审查”对应流程（`work.tasks.objective_plan.save`；具体对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.tasks.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 流程处理资格：可作为“创建工作节点”所发起流程的处理人（`work.tasks.item.create`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“更新工作节点”所发起流程的处理人（`work.tasks.item.update`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交工作汇报/绩效确认”所发起流程的处理人（`work.tasks.report.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交修订/更正”所发起流程的处理人（`work.tasks.revision.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门期初目标提交”所发起流程的处理人（`work.tasks.goal.department.objective.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人期初目标提交”所发起流程的处理人（`work.tasks.goal.personal.objective.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门考核结果提交”所发起流程的处理人（`work.tasks.goal.department.report.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人考核结果提交”所发起流程的处理人（`work.tasks.goal.personal.report.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门期初目标修订”所发起流程的处理人（`work.tasks.goal.department.objective.revise`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人期初目标修订”所发起流程的处理人（`work.tasks.goal.personal.objective.revise`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“部门考核结果修订”所发起流程的处理人（`work.tasks.goal.department.report.correct`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“个人考核结果修订”所发起流程的处理人（`work.tasks.goal.personal.report.correct`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交部门协作”所发起流程的处理人（`work.tasks.collaboration.submit`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“提交目标审查”所发起流程的处理人（`work.tasks.objective_plan.save`；具体处理接口和对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.tasks.reject`<br>审批驳回 | 驳回流程或业务申请；approve 不会自动包含 reject。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `work.tasks.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 周期与流程配置（`work.tasks.cycleFlow`）

类型：独立 capability · 页面：`/work/me` · owner：`work.tasks` · scope：全局

资源说明：Global cycle, workflow and reporting configuration capability; owner entry is still required and scoped Work grants do not imply configuration access.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `work.tasks.cycleFlow.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `work.tasks.cycleFlow.configure`<br>配置 | 修改规则、流程或系统配置；默认不自动包含业务数据读写。 | 直接执行：保存周期与流程设置（`work.tasks.okr_control.save`；PUT /api/modules/work/tasks/okr-control） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 文档中心（`docs`）

#### 文档中心（`docs`）

类型：文档资源 · 页面：`/docs` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `docs.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `docs.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `docs.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 公司管理（`docs.company`）

类型：文档资源 · 页面：`/docs/company` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `docs.company.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `docs.company.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `docs.company.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 模板编辑器（`docs.editor`）

类型：业务资源 · 页面：`/docs/editor` · scope：`personal`、`company`、`committee`、`department`

资源说明：Template create/update/delete/archive/export APIs require matching resource actions, then docs-editor space roles narrow object access; revise/submit/approve drive template workflow for organization spaces.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `docs.editor.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `docs.editor.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `docs.editor.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建文档模板草稿（`docs.editor.template.draft.create`；POST /api/modules/docs/editor）<br>直接执行：复制文档模板（`docs.editor.template.copy`；POST /api/modules/docs/editor/templates/:templateId/copy） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：保存文档模板草稿（`docs.editor.template.draft.save`；PUT /api/modules/docs/editor/templates/:templateId）<br>直接执行：发布文档模板（`docs.editor.template.publish`；POST /api/modules/docs/editor/templates/:templateId/publish） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：归档文档模板（`docs.editor.template.archive`；POST /api/modules/docs/editor/templates/:templateId/archive）<br>直接执行：删除文档模板草稿（`docs.editor.template.draft.delete`；DELETE /api/modules/docs/editor/templates/:templateId） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.archive`<br>归档 | 归档或反归档仍需保留的历史事实，不等于删除。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.reverse`<br>撤销 | 撤销、作废、冲销或执行反向业务处理；具体结果必须看资源内的业务动作。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 流程发起资格：可发起“创建文档模板草稿”对应流程（`docs.editor.template.draft.create`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“保存文档模板草稿”对应流程（`docs.editor.template.draft.save`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“发布文档模板”对应流程（`docs.editor.template.publish`；具体对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 流程处理资格：可作为“创建文档模板草稿”所发起流程的处理人（`docs.editor.template.draft.create`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“保存文档模板草稿”所发起流程的处理人（`docs.editor.template.draft.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“发布文档模板”所发起流程的处理人（`docs.editor.template.publish`；具体处理接口和对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.reject`<br>审批驳回 | 驳回流程或业务申请；approve 不会自动包含 reject。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `docs.editor.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 智能体（`agent`）

#### 智能体（`agent`）

类型：无页面运行资源 · 页面：`/agent` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `agent.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `agent.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `agent.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `agent.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 生产管理（`production`）

#### 生产管理（`production`）

类型：容器资源 · 页面：`/production` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `production.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `production.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `production.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `production.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `production.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `production.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 产品主档（`production.products`）

类型：业务资源 · 页面：`/production/products` · scope：全局

资源说明：产品与 SKU 的主数据维护入口；来源映射与历史业务引用不允许通过本资源硬删除。

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `production.products.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `production.products.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `production.products.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建产品（`production.products.product.create`；POST /api/modules/production/products）<br>直接执行：创建产品 SKU（`production.products.sku.create`；POST /api/modules/production/products/:id/skus） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `production.products.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新产品（`production.products.product.update`；PATCH /api/modules/production/products/:id）<br>直接执行：更新产品 SKU（`production.products.sku.update`；PATCH /api/modules/production/products/skus/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `production.products.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 批次检验（`production.qc`）

类型：业务资源 · 页面：`/production/qc` · scope：全局

资源说明：create=batch creation, update=inspection/precheck saves, approve=review approval, export=QC list export.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `production.qc.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `production.qc.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `production.qc.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建 QC 批次（`production.qc.batch.create`；POST /api/modules/production/qc） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `production.qc.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：保存 QC 预检记录（`production.qc.batch.precheck.save`；PATCH /api/modules/production/qc/:batchId）<br>直接执行：保存 QC 检验记录（`production.qc.batch.inspection.save`；PATCH /api/modules/production/qc/:batchId） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `production.qc.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除 QC 批次（`production.qc.batch.delete`；DELETE /api/modules/production/qc/:batchId） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `production.qc.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 流程发起资格：可发起“保存 QC 预检记录”对应流程（`production.qc.batch.precheck.save`；具体对象范围仍由 workflow/service 决定）<br>流程发起资格：可发起“保存 QC 检验记录”对应流程（`production.qc.batch.inspection.save`；具体对象范围仍由 workflow/service 决定） | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `production.qc.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 直接执行：QC 批次复核（`production.qc.batch.review`；POST /api/modules/production/qc/:batchId/approve-review）<br>流程处理资格：可作为“保存 QC 预检记录”所发起流程的处理人（`production.qc.batch.precheck.save`；具体处理接口和对象范围仍由 workflow/service 决定）<br>流程处理资格：可作为“保存 QC 检验记录”所发起流程的处理人（`production.qc.batch.inspection.save`；具体处理接口和对象范围仍由 workflow/service 决定） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `production.qc.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `production.qc.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 行政管理（`administration`）

#### 行政管理（`administration`）

类型：容器资源 · 页面：`/administration` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `administration.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `administration.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `administration.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `administration.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `administration.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `administration.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 合同台账（`administration.contracts`）

类型：业务资源 · 页面：`/administration/contracts` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `administration.contracts.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `administration.contracts.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `administration.contracts.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建行政合同（`administration.contract.create`；POST /api/modules/administration/contracts） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `administration.contracts.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新行政合同（`administration.contract.update`；PATCH /api/modules/administration/contracts/:id）<br>直接执行：创建合同修订（`administration.contract.revision.create`；POST /api/modules/administration/contracts/:id/revisions）<br>直接执行：发布合同修订（`administration.contract.revision.publish`；POST /api/modules/administration/contracts/:id/revisions/:revisionId/publish）<br>直接执行：变更合同状态（`administration.contract.state.transition`；POST /api/modules/administration/contracts/:id/state-events）<br>直接执行：冲销合同状态事件（`administration.contract.state.reverse`；POST /api/modules/administration/contracts/:id/state-events/:eventId/reverse）<br>直接执行：上传合同附件（`administration.contract.attachment.upload`；POST /api/modules/administration/contracts/:id/attachments）<br>直接执行：新增合同归档记录（`administration.contract.record.create`；POST /api/modules/administration/contracts/:id/records）<br>直接执行：登记合同审批引用（`administration.contract.approvalReference.set`；PUT /api/modules/administration/contracts/:id/approval-reference）<br>直接执行：移除合同附件（`administration.contract.attachment.remove`；POST /api/modules/administration/contracts/:id/attachments/:attachmentUid/remove） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `administration.contracts.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除行政合同（`administration.contract.delete`；DELETE /api/modules/administration/contracts/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `administration.contracts.archive`<br>归档 | 归档或反归档仍需保留的历史事实，不等于删除。 | 直接执行：归档行政合同（`administration.contract.archive`；POST /api/modules/administration/contracts/:id/archive） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `administration.contracts.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | 直接执行：下载行政合同台账（`administration.contract.export`；GET /api/modules/administration/contracts/export） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `administration.contracts.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### ERP流程尽调（`administration.erpDiligence`）

类型：业务资源 · 页面：`/administration/erp-diligence` · scope：全局

资源说明：Ordinary users can read and update only their own ERP diligence submission; the service enforces record ownership.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `administration.erpDiligence.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `administration.erpDiligence.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `administration.erpDiligence.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：保存ERP流程尽调（`administration.erpDiligence.save`；PUT /api/modules/administration/erp-diligence）<br>直接执行：上传ERP尽调样表附件（`administration.erpDiligence.evidence.upload`；POST /api/modules/administration/erp-diligence/attachments）<br>直接执行：删除ERP尽调样表附件（`administration.erpDiligence.evidence.delete`；DELETE /api/modules/administration/erp-diligence/attachments/:attachmentUid） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `administration.erpDiligence.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### ERP尽调全量查看（`administration.erpDiligence.viewAll`）

类型：独立 capability · 页面：`/administration/erp-diligence` · owner：`administration.erpDiligence` · scope：全局

资源说明：Allows reading all ERP diligence submissions after the administration.erpDiligence owner entry gate; ordinary users remain limited to their own submission.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `administration.erpDiligence.viewAll.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `administration.erpDiligence.viewAll.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 设置（`settings`）

#### Agent 助手调用（`agent.assistant`）

类型：独立 capability · 页面：`/settings/account` · owner：`settings.account` · scope：全局

资源说明：Headless toolbar and /api/agent capability; the runtime can only discover and call registered protected /api/modules business APIs.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `agent.assistant.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `agent.assistant.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `agent.assistant.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 直接执行：确认执行 Agent 业务 API 写入（`agent.businessApi.mutation.execute`；POST /api/agent/proposals/:id/confirm） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `agent.assistant.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 法定主体治理（`party.identity`）

类型：无页面运行资源 · 页面：`/settings/admin` · owner：`settings.admin` · scope：全局

资源说明：Restricted shared legal-identity governance capability; ordinary customer, supplier, company, and ownership entry points keep their own business permissions.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `party.identity.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `party.identity.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `party.identity.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 设置（`settings`）

类型：容器资源 · 页面：`/settings` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `settings.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `settings.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 账号与接入（`settings.account`）

类型：业务资源 · 页面：`/settings/account` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.account.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `settings.account.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 直接执行：设置个人通知订阅（`settings.account.notificationSubscription.save`；PUT /api/modules/settings/account/notification-subscriptions/:eventKey、DELETE /api/modules/settings/account/notification-subscriptions/:eventKey） | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `settings.account.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `settings.account.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `settings.account.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 个人 API 使用（`settings.account.apiAccess`）

类型：独立 capability · 页面：`/settings/account` · owner：`settings.account` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.account.apiAccess.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `settings.account.apiAccess.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `settings.account.apiAccess.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `settings.account.apiAccess.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 系统管理（`settings.admin`）

类型：业务资源 · 页面：`/settings/admin` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.admin.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `settings.admin.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `settings.admin.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `settings.admin.configure`<br>配置 | 修改规则、流程或系统配置；默认不自动包含业务数据读写。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `settings.admin.audit`<br>审计 | 查看审计、变更或权限台账；默认不自动包含业务数据读写。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 流程管理（`settings.admin.workflow`）

类型：独立 capability · 页面：`/settings/admin` · owner：`settings.admin` · scope：全局

资源说明：Workflow management authorization only; does not grant business submit or process actions.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.admin.workflow.configure`<br>配置 | 修改规则、流程或系统配置；默认不自动包含业务数据读写。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### API 接入（`settings.api`）

类型：业务资源 · 页面：`/settings/api` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.api.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `settings.api.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `settings.api.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `settings.api.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### Open API Client 管理（`settings.api.manage`）

类型：独立 capability · 页面：`/settings/api` · owner：`settings.api` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.api.manage.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `settings.api.manage.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `settings.api.manage.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `settings.api.manage.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `settings.api.manage.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `settings.api.manage.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### UI 组件库（`settings.ui`）

类型：文档资源 · 页面：`/settings/ui` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `settings.ui.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `settings.ui.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `settings.ui.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 财务管理（`finance`）

#### 财务管理（`finance`）

类型：容器资源 · 页面：`/finance` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `finance.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `finance.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `finance.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `finance.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `finance.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 管理会计（`finance.analysis`）

类型：业务资源 · 页面：`/finance/analysis` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.analysis.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.analysis.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.analysis.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 资产会计（`finance.assets`）

类型：业务资源 · 页面：`/finance/assets` · scope：全局

资源说明：Asset accounting owns asset cards, period depreciation and amortization, impairment and disposal evidence, read-only historical adjustments, and its workbook export independently from the general ledger; source reconciliation is limited to the initial import gate.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.assets.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.assets.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.assets.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建资产卡片（`finance.assets.asset.create`；POST /api/modules/finance/assets） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.assets.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新资产卡片（`finance.assets.asset.update`；PUT /api/modules/finance/assets）<br>直接执行：更新资产会计政策（`finance.assets.categoryPolicy.update`；PUT /api/modules/finance/assets/policies）<br>直接执行：删除资产会计政策覆盖（`finance.assets.categoryPolicy.delete`；DELETE /api/modules/finance/assets/policies） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.assets.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | 直接执行：重算折旧摊销期间（`finance.assets.assetPeriod.recalculate`；POST /api/modules/finance/assets/periods/recalculate）<br>直接执行：关联折旧摊销凭证（`finance.assets.assetPeriod.linkVoucher`；PUT /api/modules/finance/assets/periods/voucher-link）<br>直接执行：确认资产取得证据（`finance.assets.acquisitionEvidence.confirm`；POST /api/modules/finance/assets/acquisition-evidence）<br>直接执行：确认资产减值评估（`finance.assets.impairmentAssessment.confirm`；PUT /api/modules/finance/assets/impairment-assessment）<br>直接执行：确认资产处置（`finance.assets.disposal.confirm`；POST /api/modules/finance/assets/disposals） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.assets.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | 直接执行：下载资产会计 Excel（`finance.assets.workspace.export`；GET /api/modules/finance/assets/export） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.assets.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 预算管理（`finance.budget`）

类型：业务资源 · 页面：`/finance/budget` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.budget.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.budget.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.budget.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建预算版本（`finance.budget.version.create`；POST /api/modules/finance/budget/versions） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.budget.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 直接执行：启用预算版本（`finance.budget.version.activate`；POST /api/modules/finance/budget/versions/:id/activate） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.budget.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 成本管理（`finance.cost`）

类型：业务资源 · 页面：`/finance/cost` · scope：全局

资源说明：Current cost APIs are read models plus import history deletion; ingestion is script/import scoped, not general create/update CRUD.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.cost.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.cost.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.cost.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除成本导入批次（`finance.cost.import.delete`；DELETE /api/modules/finance/cost/imports/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.cost.import`<br>导入 | 导入、摄取或批量确认外部数据；默认不自动包含 create/update。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `finance.cost.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.cost.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 总账会计（`finance.ledger`）

类型：业务资源 · 页面：`/finance/ledger` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.ledger.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.ledger.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.ledger.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建会计科目（`finance.ledger.account.create`；POST /api/modules/finance/ledger/accounts）<br>直接执行：创建凭证（`finance.ledger.voucher.create`；POST /api/modules/finance/ledger/vouchers）<br>直接执行：创建会计期间（`finance.ledger.period.create`；POST /api/modules/finance/ledger/periods）<br>直接执行：初始化默认账套（`finance.ledger.defaultBook.create`；POST /api/modules/finance/ledger/init）<br>直接执行：开启关账工作台（`finance.ledger.close.open`；POST /api/modules/finance/ledger/closing） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.ledger.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新会计科目（`finance.ledger.account.update`；PUT /api/modules/finance/ledger/accounts/:id）<br>直接执行：更新凭证（`finance.ledger.voucher.update`；PUT /api/modules/finance/ledger/vouchers/:id）<br>直接执行：更新会计期间（`finance.ledger.period.update`；PUT /api/modules/finance/ledger/periods/:id）<br>直接执行：刷新关账工作台（`finance.ledger.close.refresh`；POST /api/modules/finance/ledger/closing/refresh）<br>直接执行：保存关账底稿（`finance.ledger.close.workpaper.save`；PUT /api/modules/finance/ledger/closing/workpapers） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.ledger.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除会计科目（`finance.ledger.account.delete`；DELETE /api/modules/finance/ledger/accounts/:id）<br>直接执行：删除凭证（`finance.ledger.voucher.delete`；DELETE /api/modules/finance/ledger/vouchers/:id）<br>直接执行：删除会计期间（`finance.ledger.period.delete`；DELETE /api/modules/finance/ledger/periods/:id）<br>直接执行：删除集团科目（`finance.ledger.groupAccount.delete`；DELETE /api/modules/finance/ledger/group-accounts/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.ledger.revise`<br>修订 | 对已生效、已提交或有历史版本的对象进行受控修订、重开或更正。 | 直接执行：重算科目余额（`finance.ledger.balance.revise`；POST /api/modules/finance/ledger/balances）<br>直接执行：新增集团科目（`finance.ledger.groupAccount.create`；POST /api/modules/finance/ledger/group-accounts）<br>直接执行：编辑集团科目（`finance.ledger.groupAccount.update`；PUT /api/modules/finance/ledger/group-accounts/:id）<br>直接执行：保存集团科目映射（`finance.ledger.groupAccountMapping.save`；PUT /api/modules/finance/ledger/group-accounts）<br>直接执行：保存重分类规则（`finance.ledger.reclassRule.save`；PUT /api/modules/finance/ledger/reclass-rules）<br>直接执行：保存重分类调整（`finance.ledger.reclassAdjustment.save`；PUT /api/modules/finance/ledger/reclass-adjustments）<br>直接执行：生成重分类结果（`finance.ledger.reclassResult.generate`；POST /api/modules/finance/ledger/reclass-results）<br>直接执行：调整重分类结果（`finance.ledger.reclassResult.adjust`；PATCH /api/modules/finance/ledger/reclass-results/:id） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.ledger.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 直接执行：复核集团科目（`finance.ledger.groupAccount.review`；POST /api/modules/finance/ledger/group-accounts/:id/review）<br>直接执行：复核关账底稿（`finance.ledger.close.workpaper.review`；POST /api/modules/finance/ledger/closing/workpapers/review） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.ledger.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | 直接执行：下载总账 Excel（`finance.ledger.workspace.export`；GET /api/modules/finance/ledger/export） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.ledger.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 空间经营分析（`finance.operationalAnalytics`）

类型：业务资源 · 页面：`/finance/cost` · owner：`finance.cost` · scope：`personal`、`department`、`project`

资源说明：Space-scoped operational analytics. read controls interactive queries; configure controls workspace-owned analysis templates; export is the stronger file-extraction grant and structurally includes read; apiUse is orthogonal and must be checked in addition to read for API-key access.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.operationalAnalytics.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `finance.operationalAnalytics.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry` |
| `finance.operationalAnalytics.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.operationalAnalytics.apiUse`<br>API 调用 | 通过集成 API 使用该资源能力；默认不自动包含业务读写。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `finance.operationalAnalytics.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `finance.operationalAnalytics.configure`<br>配置 | 修改规则、流程或系统配置；默认不自动包含业务数据读写。 | 直接执行：创建经营分析模板草稿（`finance.operationalAnalytics.template.draft.create`；POST /api/modules/finance/cost/operational-analytics/spaces/:targetType/:targetId/templates）<br>直接执行：修订经营分析模板草稿（`finance.operationalAnalytics.template.draft.update`；PUT /api/modules/finance/cost/operational-analytics/spaces/:targetType/:targetId/templates/:templateId）<br>直接执行：管理经营分析模板版本（`finance.operationalAnalytics.template.lifecycle`；POST /api/modules/finance/cost/operational-analytics/spaces/:targetType/:targetId/templates/:templateId/lifecycle） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 财务报表（`finance.statements`）

类型：业务资源 · 页面：`/finance/statements` · scope：全局

资源说明：The single financial-statements resource owns individual reports, one-off consolidation scope selections, and consolidation batches. create/update prepare evidence, workpapers, and the selected entities for one report generation; submit/approve/reject separate preparation from review; export downloads the current three-statement workbook; lock/unlock governs the exact consolidated snapshot. Ledger reclassification remains on finance.ledger.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.statements.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.statements.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.statements.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：刷新报表汇率（`finance.statements.exchangeRate.save`；POST /api/modules/finance/statements/consolidation/exchange-rates）<br>直接执行：创建合并批次（`finance.statements.consolidationBatch.ensure`；POST /api/modules/finance/statements/consolidation/batches） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.statements.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：选择本次合并报表主体（`finance.statements.consolidationScope.save`；PUT /api/modules/finance/statements/consolidation/scope-selections）<br>直接执行：自动准备合并来源与汇率（`finance.statements.consolidationSources.save`；PUT /api/modules/finance/statements/consolidation/batches/:batchId/sources）<br>直接执行：记录合并控制结论（`finance.statements.consolidationControl.resolve`；PUT /api/modules/finance/statements/consolidation/batches/:batchId/control-decisions）<br>直接执行：编制合并抵销分录（`finance.statements.consolidationEntry.save`；POST /api/modules/finance/statements/consolidation/batches/:batchId/entries、POST /api/modules/finance/statements/consolidation/batches/:batchId/entries/generate）<br>直接执行：记录抵销税务影响（`finance.statements.consolidationTaxEffect.save`；PUT /api/modules/finance/statements/consolidation/batches/:batchId/entries/:entryId/tax-effects） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.statements.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | 直接执行：删除合并批次草稿（`finance.statements.consolidationBatch.delete`；DELETE /api/modules/finance/statements/consolidation/batches/:batchId）<br>直接执行：删除合并抵销分录草稿（`finance.statements.consolidationEntry.delete`；DELETE /api/modules/finance/statements/consolidation/batches/:batchId/entries/:entryId）<br>直接执行：删除抵销税务影响草稿（`finance.statements.consolidationTaxEffect.delete`；DELETE /api/modules/finance/statements/consolidation/batches/:batchId/entries/:entryId/tax-effects/:taxEffectId） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.statements.lock`<br>锁定 | 关闭期间、批次或写入窗口，阻止后续普通写入。 | 直接执行：锁定合并批次（`finance.statements.consolidationBatch.lock`；POST /api/modules/finance/statements/consolidation/batches/:batchId/lock） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.statements.unlock`<br>解锁 | 重新打开期间、批次或写入窗口。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.statements.submit`<br>提交 | 提交、确认或发起业务流程；不代表有权处理或通过该流程。 | 直接执行：提交合并批次（`finance.statements.consolidationBatch.submit`；POST /api/modules/finance/statements/consolidation/batches/:batchId/submit） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.statements.approve`<br>审批通过 | 对该资源执行通过、复核、启用、发布或关闭等生效决策；必须结合完整 resource.action 和下方业务绑定理解。 | 直接执行：通过合并抵销分录（`finance.statements.consolidationEntry.approve`；POST /api/modules/finance/statements/consolidation/batches/:batchId/entries/:entryId/approve）<br>直接执行：独立复核合并批次（`finance.statements.consolidationBatch.review`；POST /api/modules/finance/statements/consolidation/batches/:batchId/review）<br>直接执行：发布合并报表（`finance.statements.consolidationBatch.publish`；POST /api/modules/finance/statements/consolidation/batches/:batchId/publish） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.statements.reject`<br>审批驳回 | 驳回流程或业务申请；approve 不会自动包含 reject。 | 直接执行：退回合并抵销分录（`finance.statements.consolidationEntry.return`；POST /api/modules/finance/statements/consolidation/batches/:batchId/entries/:entryId/return）<br>直接执行：退回合并批次（`finance.statements.consolidationBatch.return`；POST /api/modules/finance/statements/consolidation/batches/:batchId/return） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.statements.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | 直接执行：下载财务三表（`finance.statements.report.export`；GET /api/modules/finance/statements/reports/export、GET /api/modules/finance/statements/consolidation/batches/:batchId/report/export） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.statements.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 税务管理（`finance.tax`）

类型：业务资源 · 页面：`/finance/tax` · scope：全局

资源说明：Tax owns tax-accrual workpapers, filings, payable-versus-paid reconciliation, and tax evidence. Related vouchers and balances remain in finance.ledger; consolidation tax effects remain in finance.statements; period close only consumes Tax status and evidence references.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.tax.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.tax.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.tax.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建税务管理记录（`finance.tax.workspace.create`；POST /api/modules/finance/tax） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.tax.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新税务管理记录（`finance.tax.workspace.update`；PUT /api/modules/finance/tax） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.tax.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.tax.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 资金管理（`finance.treasury`）

类型：业务资源 · 页面：`/finance/treasury` · scope：全局

资源说明：Treasury owns bank-account masters, bank-reconciliation evidence, loan schedules, and interest workpapers. Related vouchers and balances remain in finance.ledger; period close only consumes Treasury status and evidence references.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `finance.treasury.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `finance.treasury.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `finance.treasury.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：创建资金管理记录（`finance.treasury.workspace.create`；POST /api/modules/finance/treasury） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.treasury.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：更新资金管理记录（`finance.treasury.workspace.update`；PUT /api/modules/finance/treasury） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `finance.treasury.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `finance.treasury.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 资料库（`library`）

#### 资料库（`library`）

类型：容器资源 · 页面：`/library` · scope：全局

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `library.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `library.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `library.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `library.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 基本资料（`library.basicInfo`）

类型：业务资源 · 页面：`/library/basic-info` · scope：全局

资源说明：Document removal is soft archive; scan and generated documents are import-like ingestion paths; confidentiality metadata is registered as a business action.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `library.basicInfo.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `library.basicInfo.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `library.basicInfo.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：保存资料元数据（`library.basicInfo.document.metadata.save`；PATCH /api/modules/library/basic-info/documents/:id） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `library.basicInfo.archive`<br>归档 | 归档或反归档仍需保留的历史事实，不等于删除。 | 直接执行：归档资料文件（`library.basicInfo.document.archive`；DELETE /api/modules/library/basic-info/documents/:id） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `library.basicInfo.import`<br>导入 | 导入、摄取或批量确认外部数据；默认不自动包含 create/update。 | 直接执行：上传资料入库（`library.basicInfo.document.import`；POST /api/modules/library/basic-info/documents）<br>直接执行：确认资料入库（`library.basicInfo.document.review`；POST /api/modules/library/basic-info/documents/:id/review）<br>直接执行：扫描资料入库（`library.basicInfo.scan.import`；POST /api/modules/library/basic-info/scan）<br>直接执行：生成资料入库（`library.basicInfo.generatedDocument.import`；POST /api/modules/library/basic-info/generated-sources/:key/generate）<br>直接执行：上传资料新版本（`library.basicInfo.documentVersion.import`；POST /api/modules/library/basic-info/documents/:id/versions） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `library.basicInfo.export`<br>导出 | 导出、下载、打印或对外发送该资源数据。 | 直接执行：生成资料集合包（`library.basicInfo.documentSet.export`；POST /api/modules/library/basic-info/exports）<br>直接执行：下载资料文件（`library.basicInfo.document.export`；GET /api/modules/library/basic-info/documents/:id/download、GET /api/modules/library/basic-info/documents/:id/versions/:versionId/download、GET /api/modules/library/basic-info/:*） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | `entry`、`read` |
| `library.basicInfo.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |
| `library.basicInfo.configure`<br>配置 | 修改规则、流程或系统配置；默认不自动包含业务数据读写。 | 直接执行：新建资料文件夹（`library.basicInfo.directory.create`；POST /api/modules/library/basic-info/directories）<br>直接执行：重命名资料文件夹（`library.basicInfo.directory.rename`；PATCH /api/modules/library/basic-info/directories）<br>直接执行：删除资料文件夹（`library.basicInfo.directory.delete`；POST /api/modules/library/basic-info/directories/delete）<br>直接执行：永久删除资料文件（`library.basicInfo.document.delete`；POST /api/modules/library/basic-info/documents/:id/delete）<br>直接执行：调整资料保密等级（`library.basicInfo.document.confidentiality.configure`；PATCH /api/modules/library/basic-info/documents/:id） | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

### 资本证券（`capitalSecurities`）

#### 资本证券（`capitalSecurities`）

类型：容器资源 · 页面：`/capital-securities` · scope：全局

资源说明：Capital Securities owns investor relations, legal-company master data, equity/control relationships, and G-line organization governance.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `capitalSecurities.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | 无 |
| `capitalSecurities.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry` |
| `capitalSecurities.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `capitalSecurities.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 页面/API guard（无独立 BusinessAction）。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `capitalSecurities.delete`<br>删除 | 删除记录；可能是硬删除，也可能由具体业务动作定义为受控删除，不能理解成编辑权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源配置或由系统规则派生；不从父资源继承。 | `entry`、`read` |
| `capitalSecurities.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 治理架构（`capitalSecurities.governance`）

类型：业务资源 · 页面：`/capital-securities/governance` · scope：全局

资源说明：create/update cover G-line organizations, legal-company master data, and the authoritative equity event ledger. OwnershipInterest remains a read-only projection; Finance report-generation scope choices never write this resource. Position, position-description, and employee-assignment writes remain under HR.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `capitalSecurities.governance.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `capitalSecurities.governance.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `capitalSecurities.governance.create`<br>新建 | 创建该资源中的新记录或业务草稿。 | 直接执行：新建治理组织（`capitalSecurities.governance.organization.create`；POST /api/modules/capitalSecurities/governance/organizations）<br>直接执行：新建公司（`capitalSecurities.governance.company.create`；POST /api/modules/capitalSecurities/governance/companies） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `capitalSecurities.governance.update`<br>编辑 | 修改该资源中已经存在且当前状态允许编辑的记录。 | 直接执行：保存治理组织（`capitalSecurities.governance.organization.save`；PUT /api/modules/capitalSecurities/governance/organizations）<br>直接执行：更新公司（`capitalSecurities.governance.company.update`；PUT /api/modules/capitalSecurities/governance/companies）<br>直接执行：重建股权投影（`capitalSecurities.governance.ownershipProjection.rebuild`；POST /api/modules/capitalSecurities/governance/ownership-projections/rebuild） | 可在当前资源配置，也可能从父资源继承。 | `entry`、`read` |
| `capitalSecurities.governance.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

#### 投资人关系（`capitalSecurities.investors`）

类型：业务资源 · 页面：`/capital-securities/investors` · scope：全局

资源说明：Read-only temporal projection over OwnershipInterest; current and historical Captables are generated by issuer and as-of date, while pending changes remain traceable but non-effective.

| Action | 通用含义 | 直接动作 / 流程资格 | 配置与继承 | 自动包含 |
|---|---|---|---|---|
| `capitalSecurities.investors.entry`<br>进入 | 进入资源对应的页面、菜单或功能入口。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | 无 |
| `capitalSecurities.investors.read`<br>查看 | 查看该资源允许暴露的列表、详情和只读数据；对象范围仍由 service 与 scope 限制。 | 页面/API guard（无独立 BusinessAction）。 | 可在当前资源配置，也可能从父资源继承。 | `entry` |
| `capitalSecurities.investors.grant`<br>授权 | 管理其他用户、岗位或部门在该资源上的授权；不是业务数据管理员权限。 | ⚠ 未登记具体 BusinessAction；授权前必须继续核对页面/API guard 和 service。 | 当前资源显式配置；不从父资源继承。可授予用户、岗位或部门。 | 无 |

## 派生空间与流程配置资源

这些资源同样属于完整权限集合，但其 action 语义来自空间投影或流程配置树，不重复展开逐 action 表。空间权限必须连同具体 `scopeId` 判断；流程配置 capability 只控制流程规则，不授予业务数据审批权。

| 资源 | 名称 | 类型 | Actions | 来源 / owner |
|---|---|---|---|---|
| `settings.admin.workflow.action.docs.editor.template.draft.create` | 创建文档模板草稿 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.docs.editor.template.draft.save` | 保存文档模板草稿 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.docs.editor.template.publish` | 发布文档模板 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.hr.performance.review.evaluate` | 发起绩效评审 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.hr.roster.department.create` | 创建部门 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.hr.roster.department.update` | 更新部门 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.inventory.receipts.report.confirm` | 确认成品入库报单月度汇总 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.production.qc.batch.inspection.save` | 保存 QC 检验记录 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.production.qc.batch.precheck.save` | 保存 QC 预检记录 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.projects.project.create` | 提交项目确认 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.collaboration.submit` | 提交部门协作 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.department.objective.revise` | 部门期初目标修订 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.department.objective.submit` | 部门期初目标提交 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.department.report.correct` | 部门考核结果修订 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.department.report.submit` | 部门考核结果提交 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.personal.objective.revise` | 个人期初目标修订 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.personal.objective.submit` | 个人期初目标提交 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.personal.report.correct` | 个人考核结果修订 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.goal.personal.report.submit` | 个人考核结果提交 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.item.create` | 创建工作节点 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.item.update` | 更新工作节点 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.objective_plan.save` | 提交目标审查 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.report.save` | 提交工作汇报/绩效确认 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.action.work.tasks.revision.save` | 提交修订/更正 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.administration` | 行政流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.assessment` | 考核流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.collaboration` | 协作流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.contract` | 合同流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.document` | 文档流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.finance` | 财务流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.hr` | 人事流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.it` | IT 流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.procurement` | 采购流程 | 独立 capability | `configure` | `settings.admin` |
| `settings.admin.workflow.category.quality` | 质量流程 | 独立 capability | `configure` | `settings.admin` |
| `space.committee` | 委员会空间 | 容器资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`export`、`grant` | `系统派生` |
| `space.committee.projects` | 项目 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`revise`、`submit`、`approve`、`reject`、`grant` | `work.projects` |
| `space.committee.tasks` | 任务 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`grant` | `work.tasks` |
| `space.committee.templates` | 模板 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`export`、`grant` | `docs.editor` |
| `space.company` | 公司空间 | 容器资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`export`、`grant` | `系统派生` |
| `space.company.projects` | 项目 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`revise`、`submit`、`approve`、`reject`、`grant` | `work.projects` |
| `space.company.tasks` | 任务 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`grant` | `work.tasks` |
| `space.company.templates` | 模板 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`export`、`grant` | `docs.editor` |
| `space.department` | 部门空间 | 容器资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`export`、`apiUse`、`grant`、`configure` | `系统派生` |
| `space.department.analytics` | 经营分析 | 业务资源 | `entry`、`read`、`export`、`apiUse`、`grant`、`configure` | `finance.operationalAnalytics` |
| `space.department.projects` | 项目 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`revise`、`submit`、`approve`、`reject`、`grant` | `work.projects` |
| `space.department.tasks` | 任务 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`grant` | `work.tasks` |
| `space.department.templates` | 模板 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`export`、`grant` | `docs.editor` |
| `space.project` | 项目空间 | 容器资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`export`、`apiUse`、`grant`、`configure` | `系统派生` |
| `space.project.analytics` | 经营分析 | 业务资源 | `entry`、`read`、`export`、`apiUse`、`grant`、`configure` | `finance.operationalAnalytics` |
| `space.project.tasks` | 任务 | 业务资源 | `entry`、`read`、`create`、`update`、`delete`、`archive`、`revise`、`reverse`、`submit`、`approve`、`reject`、`grant` | `work.tasks` |
